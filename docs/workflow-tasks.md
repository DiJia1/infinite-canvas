# Workflow 实施任务

依据：[架构与实施方案](workflow-implementation-plan.md)。每个任务在针对性测试和差异检查通过后独立提交；不推送、不部署、不调用收费生成。阶段中的模拟验证不能替代正式执行链路。

| Task | 依赖 | 范围 | 完成验证 | 状态 |
| --- | --- | --- | --- | --- |
| T0 | 无 | 固定实施计划、任务拆分及契约审查 | 计划与需求逐项核对 | 已完成（956e676；契约按代码落实） |
| T1 | T0 | Workflow 定义、稳定槽位与连线类型、校验、持久化模型和 CRUD | 草稿、9 进 9 出、DAG、权限、乐观锁、复制删除 | 已完成 |
| T2 | T1 | Workflow 媒体引用与既有清理协议集成 | 事务、并发清理、运行引用、删除释放 | 已完成 |
| T3 | T1 | 流程库、导航及独立编辑器；共享节点、参数和资源展示 | 保存重开、拖动连线、无外部名称、图片视频配置；Chrome 图片加载与本地视频播放 | 已完成 |
| T4a | T0 | 既有图片任务领取令牌、租约与写回保护；提交待确认状态 | 供应商接受后崩溃不重提；旧 Worker 不得覆盖结果；媒体与成功状态原子入库 | 已完成 |
| T4 | T1、T2、T4a | 运行快照、尝试记录、数据库调度、容量与租约、图片任务接入 | 分支汇合、幂等、崩溃恢复、容量竞争与旧 Worker 拒写 | 已完成 |
| T4b | T4 | 普通生成接口隔离 Workflow 保留请求标识，防止运行结果被其他请求占用 | 图片生成、图片编辑、视频创建拒绝保留前缀且不创建任务 | 已完成 |
| T5 | T4 | 视频任务接入、混合流程、Stop、失败槽位 Retry 与待确认恢复 | 图片→视频、视频参考、局部重试、停止收尾；历史 Retry 幂等与租约竞争 | 已完成 |
| T6 | T3、T5 | 编辑器运行状态、运行历史、图片重试入口与后端完整接入 | Chrome 后台运行恢复、503 接受后丢响应不重复提交、图片重试及视频历史预览 | 已完成 |
| T7 | T2、T6 | 独立复审、端到端回归、迁移与上线说明 | Go 全量、Bun 462 项、TypeScript、生产构建及 Chrome 隔离环境验收 | 已完成 |

## 执行边界

- 子代理使用 `gpt-5.6-sol`、`high`，按文件所有权分工；共享契约先确定，后续修改需同步。
- 每个 task 由主协调者核对验证、整合并串行提交，避免并行操作 Git index；子代理不自行提交其他任务文件。
- 使用现有 Go/GORM/PostgreSQL、Next.js/React/Ant Design 和 Bun；不套用门户项目技术栈。
- 自动迁移只在隔离测试数据库验证，生产迁移、备份及收费验收属于后续上线操作。
- 真实供应商生成未获授权时使用现有供应商模拟测试，明确保留真实收费验收事项。

验收与后续上线边界见：[上线与运行维护](workflow-operations.md)。所有阶段均已本地提交；未推送、未部署，未发起收费生成。
